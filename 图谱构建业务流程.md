# 图谱构建业务流程（迭代版）

## 1. 文档目标

本文件用于明确“用户如何从业务资料出发，完成本体定义与图谱构建”，并作为后续迭代开发的统一基线。  
覆盖范围：项目创建、数据接入、Schema 设计、图谱构建、质量校验、发布应用、持续迭代。

## 2. 角色与职责

- 业务负责人：定义业务目标、验收图谱效果
- 领域专家：审核本体概念、关系语义、规则正确性
- 平台用户（实施人员）：执行上传、配置、触发构建、查看结果
- 系统能力（平台 + 模型）：解析文档、抽取结构、构建与更新图谱

## 3. 核心业务对象

- 项目（Project）：一次业务建模的容器，隔离数据与配置
- 数据源（Data Source）：本地文档或远程数据库连接
- 文档资产（Document Asset）：pdf/xlsx 等原始输入及其解析结果
- 本体 Schema：实体类型、属性、关系类型及约束
- 图谱实例（Graph Instance）：按 Schema 构建出的节点与边
- 构建任务（Build Job）：一次可追踪、可重试的图谱构建执行

## 4. 端到端业务流程

### 阶段 A：项目初始化

1. 用户创建项目，填写项目名称、业务域、负责人、描述。
2. 系统初始化项目空间与默认配置（状态：`草稿`）。

输出：`ProjectId`

### 阶段 B：数据接入与管理

1. 用户在项目下添加数据源：
   - 方式 1：上传文档（`xlsx`、`pdf`）
   - 方式 2：配置远程数据源（数据库级连接信息）
2. 系统做接入校验：
   - 文件格式与大小校验
   - 远程连接可达性与权限校验
3. 对 `pdf` 执行文本解析，对 `xlsx` 执行结构化读取，生成可抽取文本与元数据。

输出：标准化后的可处理数据集（`Parsed Corpus`）

### 阶段 C：本体 Schema 定义

1. 用户选择 Schema 生成方式：
   - 手工定义：人工维护实体、属性、关系
   - AI 辅助生成：基于项目内文档自动提炼候选 Schema
2. 领域专家审核并修订候选 Schema。
3. 系统保存版本化 Schema（建议支持 `v1/v2/...`）。

输出：已审核可用的 `SchemaVersion`

### 阶段 D：图谱构建

1. 用户选择 `SchemaVersion` + 数据范围，触发构建任务。
2. 系统执行抽取流程：
   - 实体抽取
   - 关系抽取
   - 属性映射
   - 规则校验（类型、必填、基数、引用完整性）
3. 通过外部模型 API/算法服务进行语义补全与消歧。
4. 生成图谱实例并写入图数据库/图存储。

输出：`GraphBuildResult`（成功数、失败数、异常明细）

### 阶段 E：质量校验与发布

1. 系统输出质量报告：
   - 抽取覆盖率
   - 关系正确率（抽样）
   - 冲突/重复/孤立节点统计
2. 用户确认后发布图谱版本（状态：`已发布`）。

输出：可查询、可诊断、可视化的线上图谱版本

### 阶段 F：业务应用与闭环优化

1. 业务方在检索、诊断、问答场景中使用图谱。
2. 系统记录低置信度结果、人工修正记录、错误样本。
3. 周期性回流到 Schema 和抽取策略，形成迭代。

输出：持续优化的本体与图谱效果

## 5. 当前能力与目标能力（用于迭代对齐）

### 已具备（当前项目内）

- 本体管理能力：实体类型、关系类型、动作、函数等 CRUD
- 图谱可视化与查询入口
- 结合知识图谱的诊断分析链路

### 待补齐（与本流程直接相关）

- 项目级文档管理（上传、版本、标签、权限）
- `pdf/xlsx` 统一解析流水线与任务状态跟踪
- 远程数据源接入（数据库连接、增量同步）
- “Schema 生成 + 审核 + 版本化”完整闭环
- 构建任务编排、失败重试、质量门禁与发布流程

## 6. 建议的迭代优先级

1. P0：项目与数据接入底座（项目空间 + 文档上传 + 解析任务）
2. P0：Schema 版本化与审核流（人工/AI 双模式）
3. P1：图谱构建任务中心（可观测、可重试、可回滚）
4. P1：质量评估面板（覆盖率、冲突率、低置信样本）
5. P2：远程数据源增量同步与自动重建策略

## 7. 验收标准（建议）

- 能在一个项目内完成“上传文档 → 生成/维护 Schema → 构建图谱 → 发布”的闭环
- 任一构建任务具备可追踪状态与失败原因
- Schema 与图谱均支持版本化查询与回滚
- 发布后的图谱可被诊断与可视化模块稳定消费
